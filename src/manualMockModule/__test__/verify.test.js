import { register } from "../user";

jest.mock("../verify");

describe("verify", () => {
  test("should verify username", async () => {
    // The verifyUsername returns as expect
  });

  test("should verify password", async () => {
    // The verifyPassword returns as expect
  });
});
